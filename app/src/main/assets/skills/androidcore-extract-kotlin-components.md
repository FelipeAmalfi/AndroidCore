# AndroidCore Skill: Extract Kotlin Components

- Parse Kotlin files in source modules.
- Extract `@Composable` function signatures.
- Extract parameters and defaults.
- Extract preview annotations and state-like inputs.


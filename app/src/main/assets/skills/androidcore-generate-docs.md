# AndroidCore Skill: Generate Docs

- Generate markdown docs for components and tokens.
- Include title, description, props table, and usage examples.
- Write artifacts under `docs/.generated` for automation.


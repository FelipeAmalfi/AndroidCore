# AndroidCore Skill: Publish Dracosaurus

- Publish with CLI `dracosaurus push ./docs`.
- Command is configurable via docs-agent config.
- Local hook must log failures and continue push.
- CI retries publish failures.


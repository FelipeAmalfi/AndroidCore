# AndroidCore Skill: Build Docusaurus

- Build site with CLI `npm run build` from `website/`.
- Command is configurable via docs-agent config.
- Local hook must log failures and continue push.
- CI must install dependencies and build before deploy.




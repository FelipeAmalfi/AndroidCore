# generate-docs

## Goal
Generate markdown documentation for extracted components and tokens.

## Rules
- Create component pages with title, description, props table, usage example.
- Create token pages for colors, typography, spacing.
- Generate machine-readable artifacts in `docs/.generated`.
- Include deterministic headings and stable ordering.

